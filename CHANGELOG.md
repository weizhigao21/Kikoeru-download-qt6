# 更新日志

## v1.35.0
- **重构**：代码模块拆分 — 所有模块文件控制在 400 行以内，提升可维护性和可读性
  - `gui_app.py`（891行 → 167行）：拆分为 4 个文件 — `gui_app.py`（主入口 + 工具方法）、`gui_app_ui.py`（`UISetupMixin` 样式/UI构建）、`gui_app_nav.py`（`NavigationMixin` 数据加载/分页导航）、`gui_app_events.py`（`EventMixin` 搜索历史/快捷键/鼠标事件）
  - `src/download/manager.py`（705行 → 253行）：拆分为 4 个文件 — `models.py`（`TaskStatus`/`DownloadTask` 数据模型）、`manager.py`（`DownloadManager` 主类）、`manager_core.py`（`DownloadCoreMixin` 下载提交/持久化/队列）、`manager_poll.py`（`DownloadPollMixin` 轮询进度/重试/低速检测）
  - `src/database/database.py`（569行 → 211行）：拆分为 3 个文件 — `database.py`（`DatabaseManager`）、`history.py`（`DownloadHistoryManager`）、`pending.py`（`PendingTaskManager`）
  - `src/ui/list_mixin.py`（510行 → 184行）：拆分为 2 个文件 — `list_mixin.py`（`ListMixin` 列表展示/缩略图/canvas管理）、`list_card.py`（`ListCardMixin` 卡片创建/标签渲染/翻译交互）
  - `src/ui/detail_mixin.py`（437行 → 306行）：拆分为 2 个文件 — `detail_mixin.py`（`DetailMixin` 详情面板/展示/懒加载）、`detail_actions.py`（`DetailActionsMixin` 隐藏/刷新/删除/复制操作）
- **优化**：所有 30 个 Python 模块文件均 ≤ 366 行，最大文件为 `search_mixin.py`（366行）
- **修复**：「隐藏下载」翻页过滤失效 — 启用「隐藏下载」模式后翻页（或标签/关键词/厂商搜索翻页），新页数据从 API 加载后未重新应用过滤器导致已下载作品再次出现。修复 `_on_data_loaded`（推荐/最新列表翻页）和搜索成功回调（`_on_tag_search_success`/`_on_keyword_search_success`/`_on_circle_search_success`）共 4 条代码路径，数据加载完成后统一检查 `show_downloaded` 状态并重新过滤

## v1.34.0
- **优化**：移除列表渲染阻塞 — `display_works_list` 中移除 `update_idletasks()` 强制同步刷新调用，列表渲染不再阻塞主线程，页面切换流畅度明显提升
- **优化**：标签绘制性能 — `_draw_tags_on_canvas` 使用 `tkfont.Font.measure()` 预计算文本宽度，替代每标签创建/删除 Canvas 对象的测宽方式，减少约 100 次/页的 GC 压力
- **优化**：缩略图流式加载 — `_load_thumbnails_batch` 改用 `as_completed` 流式处理，图片加载完一张即刻显示一张，不再等全部完成后统一刷新
- **优化**：共享线程池 — `WorkApp` 初始化时创建全局 `_thumb_pool`（8线程）和 `_data_pool`（4线程），列表和筛选模块复用现有线程池，避免每次翻页都创建/销毁 `ThreadPoolExecutor`
- **优化**：直接下载线程分派 — `batch_size` 从整数除法改为 ceil 除法 `(n + max - 1) // max`，确保恰好创建 `max_threads` 个线程充分利用并行能力
- **优化**：下载管理窗口单例 — `open_download_manager` 增加已存在窗口检测，重复点击时置顶聚焦而非创建新窗口；窗口关闭时自动清理引用
- **新增**：快捷键扩展 — `Ctrl+D` / `Enter` 打开当前作品下载窗口，`Up` / `Down` 切换列表选中作品（右侧详情同步更新），`PageUp` / `PageDown` 翻页
- **新增**：动态下载进度条 — 底部下载栏按需创建任务槽位，同时显示所有并行下载任务的进度，不再限制为 1 个
- **修复**：`DownloadHistoryManager._safe_json_load` 缺失 — `get_all_downloaded_works_full` 调用 `_safe_json_load` 时因方法仅定义在 `DatabaseManager` 上导致崩溃，已在 `DownloadHistoryManager` 类补充静态方法

## v1.33.0
- **新增**：下载管理窗口 — 新增 `DownloadManagerWindow` 独立窗口，底部栏「下载管理」按钮打开；上下按钮切换「正在下载」和「已完成」两个区域；正在下载区横排显示状态/ID/名称/进度条/速度，已完成区简洁显示ID/名称+✓
- **新增**：低速自动重启 — 下载速度持续低于阈值（默认 1MB/s）超过设定时间（默认 10秒）时自动取消并重新提交任务（支持断点续传接续），单任务最多自动重启 3 次；可通过 `config.json` 的 `slow_speed_threshold`、`slow_speed_duration`、`max_slow_restarts` 配置
- **优化**：UI 刷新消除闪烁 — 下载管理窗口改为增量更新模式（对比任务 ID 集合，仅在任务列表变化时重建控件，进度更新只改文字值）；底部栏任务槽位使用可见性追踪避免重复 grid() 调用
- **优化**：底部栏布局调整 — 「下载管理」按钮移至最左侧，「翻页」按钮居中显示，「设置」按钮固定最右侧；任务区精简为单行显示（仅 ID + 百分比数字 + 速度），无进度数据时显示「提交中」/「下载中」状态提示

## v1.32.0
- **新增**：下载任务持久化 — 新增 `PendingTaskManager` 类，将未完成（submitting/downloading/queued/failed）的下载任务保存到 SQLite 数据库，程序重启后自动恢复为 FAILED 状态，用户可点击重试继续下载；已完成或取消的任务自动从持久化记录中清除
- **新增**：直接下载断点续传 — `DirectDownloader` 支持 HTTP Range 请求，文件部分下载中断后重新下载时从断点位置续传（206 Partial Content），服务端不支持时自动回退从头下载；已完整文件返回 416 时直接跳过；进度条显示真实总进度（已有字节+新下字节）
- **优化**：`DownloadManager` 新增 `restore_pending_tasks()`、`clear_pending_task()`、`clear_all_pending()` 公共方法，支持启动恢复和手动清理待处理任务

## v1.31.0
- **新增**：下载文件名字符过滤 — 设置窗口「下载设置」页新增「文件名过滤字符」输入框，可自定义额外过滤的字符（如 `【】「」《》…`），与 Windows 非法字符合并处理，避免下载目录名包含特殊字符
- **优化**：`WorkDownloader._get_save_dir()` 重构为基于集合的过滤逻辑，支持动态扩展非法字符集

## v1.30.0
- **修复**：数据库连接泄漏 — `DatabaseManager` 和 `DownloadHistoryManager` 连接超时自动关闭（5分钟未使用），启用 WAL 模式提升并发性能，添加自动 commit/rollback 事务管理
- **优化**：LRU 缓存锁竞争 — 使用 `RLock()` 可重入锁替代非阻塞锁（10ms/20ms超时），缓存命中率从 ~30% 提升至 >95%，添加命中率统计监控
- **修复**：下载管理器内存泄漏 — 任务字典添加上限（200个），已完成任务保留最近100条，定期清理机制（每10次轮询或5分钟），解决长时间运行后内存持续增长问题
- **优化**：数据库查询性能 — 明确指定列名消除 `SELECT *` 和运行时列检查，新增 `_safe_json_load()` 安全解析方法，SQL 注入防护（白名单验证排序字段）
- **优化**：UI 更新批处理 — 数据加载后合并多次重绘为单次批量操作，添加等待光标反馈，消除界面闪烁
- **新增**：图片智能预加载 — 滚动列表时预加载当前项前后各3张图片（后台线程+队列管理），用户体验接近原生应用
- **新增**：缓存效率监控 — `ImageCacheManager.get_stats()` 返回命中率、磁盘大小等指标，便于性能调优

## v1.29.0
- **新增**：详情页ID复制按钮 — ID标签旁添加复制按钮，一键复制作品RJ ID到剪贴板

## v1.28.0
- **新增**：隐藏下载作品按钮 — "推荐作品"和"最新收录"模式下支持切换"显示全部/隐藏下载"状态，快速过滤已下载作品
- **优化**：排序控件显示 — 排序下拉框仅在"下载作品"模式下显示，界面更简洁
- **优化**：翻页按钮交互 — 按钮始终显示，在第一页/最后一页时变为禁用状态而非隐藏，避免按钮位置跳动

## v1.27.0
- **修复**：下载作品重复加载 API 补全信息 — `update_work_detail` 方法更新数据库时 RJ ID 格式不匹配（带 RJ 前缀更新纯数字 ID），导致声优/厂商信息永远写不进去；修复为使用规范化后的 ID 进行 WHERE 条件匹配
- **优化**：下载作品缓存失效策略 — `_on_dl_tasks_changed` 改为仅在下任务完成或失败时使缓存失效，进度更新不再触发缓存失效，避免频繁重新加载
- **优化**：作品信息完整性判断 — 空声优数组 `[]` 和空厂商字典 `{}` 视为"已获取"而非"缺失"，避免对本身无此信息的作品重复请求 API
- **优化**：作品缓存初始化逻辑 — `_load_downloaded_works` 在加载后立即根据数据库中的完整信息初始化 `_fetched_ids`，减少不必要的 API 请求

## v1.26.0
- **新增**：三栏列表切换 — 下拉框新增"下载作品"选项，整合只看已下载功能，替代原有的循环切换按钮
- **新增**：下载作品缓存 — 切换回"下载作品"tab 时不再重复查询数据库，下载任务变更时自动失效
- **移除**：筛选按钮 — 原有的"显示全部/隐藏已下载/只看已下载"循环按钮已被下拉框替代
- **修复**：缩略图内存缓存未填充 — `_load_thumbnails_batch` 加载图片后未创建 `thumb_<url>` 缓存键，导致每次重新显示列表时需重复读磁盘
- **修复**：下载作品重复请求 API 补全信息 — `_check_and_fetch_missing_data` 对空声优/厂商/封面视为"缺失"反复重试；新增 `_fetched_ids` 集合追踪已请求过的作品，跳过重复调用

## v1.25.0
- **优化**：按钮样式 — 蓝色背景统一使用白色文字，视觉更协调
- **优化**：翻页按钮 — 第一页/最后一页时直接隐藏对应按钮，而非显示灰色禁用态
- **优化**：搜索框切换 — 标签/关键词/厂商搜索时控件复用，减少销毁重建导致的闪烁
- **修复**：设置窗口浏览按钮 — 窗口宽度增加到 700，表单列自动扩展，按钮显示完整

## v1.24.0
- **优化**：UI 颜色统一 — 创建统一的颜色方案，解决颜色块不均匀问题
- **优化**：卡片样式 — 使用白色背景和边框，更清晰的层次感
- **优化**：颜色常量 — 定义 COLORS 字典统一管理所有颜色
- **修复**：图片显示 — 修复图片容器尺寸问题，使用自适应布局

## v1.23.0
- **优化**：字体显示优化 — 使用更圆润的字体大小和样式，提升阅读体验
- **优化**：全局样式统一 — 通过 ttk.Style 统一配置所有控件字体
- **优化**：按钮内边距 — 增加按钮 padding，使按钮更易点击
- **优化**：详情面板字体 — 标签、标题等使用更大更清晰的字体

## v1.22.0
- **新增**：翻译编辑功能 — 翻译完成后显示"编辑"按钮，可手动修改翻译结果
- **新增**：翻译删除功能 — 编译窗口支持删除翻译，恢复显示原文
- **新增**：翻译编辑开关 — 设置界面可启用/禁用翻译编辑功能
- **新增**：数据库支持删除翻译 — `delete_translated_title` 方法

## v1.21.0
- **优化**：设置窗口 UI 重构 — 采用左侧导航+右侧内容布局，更清晰直观
- **新增**：设置页面分组 — 下载设置、队列设置、存储管理、AI 翻译四个独立页面
- **优化**：导航交互 — 点击左侧菜单切换右侧内容，当前页高亮显示

## v1.20.0
- **新增**：下载文件完整性检查 — 下载前通过 HEAD 请求获取远程文件大小，与本地文件对比判断是否完整
- **优化**：跳过已完整文件 — 本地文件大小与远程一致才跳过，不完整的文件会重新下载
- **优化**：控制台输出跳过信息 — 显示跳过的文件数和剩余待下载数

## v1.19.0
- **新增**：下载队列模式 — 启用后作品按顺序下载，一个完成后再开始下一个
- **新增**：队列模式配置 — 设置窗口支持启用/禁用队列模式，设置最大同时下载数
- **新增**：队列状态显示 — 底部任务列表显示排队中的作品和位置
- **优化**：直接下载进度更新 — 更新间隔从 1 秒改为 0.3 秒，进度更平滑
- **修复**：直接下载完成状态 — 修复完成后进度条不显示 100% 的问题

## v1.18.0
- **新增**：直接下载模块 — 不依赖 Aria2 的 HTTP 直接下载，可在设置中切换下载方式
- **新增**：下载方式切换 — 设置窗口支持选择 Aria2 或直接下载
- **新增**：直接下载线程数配置 — 可设置直接下载的并发线程数（1-10）
- **新增**：下载失败自动重试 — 下载失败（429 限流等）自动重试，最多 3 次，指数退避等待
- **新增**：直接下载 429 重试 — 直接下载遇到限流自动重试，最多 5 次
- **优化**：Aria2 按需启动 — 改为下载时检测并启动，而非程序启动时
- **优化**：Aria2 配置优化 — 减少并发数和连接数，降低被限流风险
- **优化**：已下载标记位置固定 — "✓ 已下载"标记固定在标题后面，不受其他控件影响
- **修复**：直接下载进度显示 — 修复完成任务不计入总进度的问题

## v1.17.0
- **修复**：详情页翻译标题不显示 — 打开详情页时自动查询数据库中的翻译结果，默认显示译文
- **新增**：详情页标题"原/译"切换按钮 — 翻译存在时显示，点击在原文和译文之间切换
- **新增**：详情页复制标题按钮 — 点击复制当前显示的标题（译文或原文）到剪贴板
- **修复**：下载窗口选择逻辑错误 — 取消选择文件夹时子文件未联动取消，导致下载了未选中的文件
- **修复**：下载窗口选择文件夹时子文件未联动选中 — 手动选择文件夹时自动选中其所有子文件
- **修复**：下载窗口选择计数不准确 — 已选择计数从"包含文件夹的总数"改为"叶子文件数"
- **修复**：翻译失败后重试无响应 — 翻译服务不再缓存空结果，失败后可正常重试；网络异常时自动重建连接
- **优化**：翻译成功/失败时底部状态栏显示明确提示
- **修复**：保存标签和下载历史时因标签 `name` 为 `None` 导致 `join` 崩溃 — 列表下标确认安全后再获取 `name`，`None` 值自动跳过

## v1.16.0
- **修复**：翻译结果持久化 — 使用独立 `translations` 表按 work_id 存储，不再依赖 `works` 表行是否存在
- **修复**：最新收录作品翻译重启后丢失 — 翻译结果保存到独立表，推荐列表和最新收录均能正确持久化
- **修复**：API 地址末尾自动去除 `/chat/completions`，避免用户填写完整地址导致 URL 重复拼接
- **修复**：翻译配置导入方式从值绑定改为模块引用，设置保存后翻译按钮立即生效
- **优化**：`save_works` 不再需要备份翻译结果，简化了页面刷新逻辑
- **优化**：`download_history` 表移除冗余的 `translated_title` 列，统一由 `translations` 表管理

## v1.15.0
- **新增**：AI 翻译功能，支持使用 OpenAI 兼容 API（如 DeepSeek、GPT 等）翻译作品标题
- **新增**：列表中复制按钮旁显示"翻译"按钮，一键翻译当前作品标题
- **新增**：翻译完成后标题旁显示"原/译"切换按钮，随时在原文和译文之间切换
- **新增**：翻译结果持久化到数据库（`translations` 表），重启后仍然生效
- **新增**：下载时如果标题显示为译文，下载目录名自动使用译文标题
- **新增**：设置窗口新增 AI 翻译设置区域（API Key、API 地址、模型名称、启用开关）
- **新增**：`src/services/translator.py` - AI 翻译服务模块（OpenAI 兼容 API、翻译缓存）
- **优化**：翻译服务使用异步线程，不阻塞 UI
- **优化**：翻译结果自动缓存，避免重复请求
- **优化**：翻译结果独立存储，刷新页面不影响已翻译的标题

## v1.14.0
- **新增**：全局下载管理器 `DownloadManager`（单例），统一管理所有下载任务的注册、提交、轮询、取消
- **新增**：多作品并行下载支持，底部任务列表同时显示多个进行中任务的进度
- **新增**：观察者模式（`add_observer`），主界面通过回调实时监听任务状态变更
- **新增**：后台 Housekeeper 线程静默执行封面下载、标签保存、下载历史写入
- **新增**：设置窗口支持自定义数据库存储位置（`db_dir` 配置项），支持路径选择和数据迁移
- **优化**：下载窗口大幅精简（去掉轮询/回调/转移逻辑），提交后 2 秒自动关闭
- **优化**：底部任务列表从单一进度条改为多行任务列表，预创建 3 个固定槽位 + `grid` 布局，零闪烁
- **优化**：任务列表更新时只改数值，不销毁重建控件
- **修复**：多标签搜索分隔符从 `$` 改为空格，多标签组合搜索不再返回 0 结果

## v1.13.1
- 修复：翻页到第3页卡死（`LRUCache.put()` 阻塞锁导致主线程被后台线程锁竞争永久阻塞，全部改为非阻塞 `acquire(blocking=False)`）
- 修复：`_on_tab_changed` 中 loading 状态被永久锁定（拦截时恢复 `tab_var`）
- 修复：`show_loading()`/`hide_loading()` 快速翻页时 TclError（`winfo_exists()` 检查 + `stop()` 停止 Progressbar 定时器）
- 修复：`_InFlight.dedup()` 竞态条件导致重复 API 请求（`evt.wait()` 等待 + `_results` 缓存）

## v1.13.0
- 优化：数据库连接池（`DatabaseManager` 和 `DownloadHistoryManager` 使用 `threading.local()` 缓存每线程连接，不再每次操作都创建/销毁连接）
- 优化：请求合并（`api_client.py` 新增 `_InFlight` 类，进行中相同请求自动共享结果，避免重复网络请求）
- 优化：批量更新（移除 `_batch_update_thumbnails` 中阻塞事件循环的 `update_idletasks()` 调用）
- 优化：批量更新（`_fetch_missing_thumbnails` 进度更新从每批创建 lambda 闭包改为共享可变状态 + 单函数引用，减少 GC 压力）

## v1.12.1
- 修复：线程安全问题（`ImageTk.PhotoImage` 改为主线程创建，`cache.py` 新增 `_load_pil_from_url` 方法返回 PIL Image）
- 修复：线程安全问题（`filter_mixin.py` 中 `StringVar.get()` 改为主线程读取后通过参数传入后台线程）
- 修复：标签换行计算不准确（`_draw_tags_on_canvas` 改为基于 Canvas 实际宽度自适应换行，替换固定每行5个的逻辑）

## v1.12.0
- 修复：下载进度查询竞态条件（`poll_download_progress` 迭代集合时删除元素导致异常，改为先收集再删除）
- 修复：数据库连接泄漏（`DatabaseManager` 和 `DownloadHistoryManager` 全部方法改用 `contextmanager` 确保连接关闭）
- 修复：`is_downloaded()` 性能问题（原来加载全部记录到内存再比较，改为 SQL 直接查询）
- 修复：所有裸 `except:` 改为 `except Exception:`，避免意外吞掉 `KeyboardInterrupt` 等系统异常
- 修复：`ImageCacheManager` 缺失 `get()` 和 `get_http_session()` 方法（重构时误删，已恢复）
- 修复：详情页图片尺寸错误（详情页现在加载 400×400 高清封面 mainCoverUrl，而非 180×180 缩略图）
- 修复：mainCoverUrl 未写入数据库（懒加载获取的高清封面 URL 现在持久化到 works 表）
- 修复：声优/厂商信息每次都重新请求 API（懒加载前先查询 works.db 和 download_history.db，命中则跳过网络请求）
- 优化：数据库添加索引（`works.work_id`、`works.page`、`download_history.rj_id`、`download_history.created_at`）
- 优化：API 请求添加结果缓存（LRU 缓存，最多 100 条，TTL 120 秒），减少重复网络请求
- 优化：API 请求添加 `requests.Session` 连接复用和固定 User-Agent 头
- 优化：图片缓存添加磁盘空间管理（默认上限 500MB，超限自动清理最旧文件）
- 优化：图片保存时自动压缩为 JPEG 85% 质量，减少磁盘占用
- 优化：图片缓存按尺寸独立管理（180×180 缩略图 + 400×400 高清图），互不干扰
- 新增：快捷键支持 — `Ctrl+F` 聚焦搜索框、`←/→` 翻页（搜索框内不触发）、`Esc` 清除搜索状态
- 新增：API 缓存刷新（搜索/翻页结果自动缓存，切换标签/关键词自动清除旧缓存）
- 新增：设置窗口显示图片缓存大小，支持一键清除缓存（内存 LRU + 磁盘文件）
- 新增：详情页懒加载完成后自动切换到 mainCoverUrl 高清大图（先显示 180×180 缩略图作为占位）

## v1.11.0
- 优化：翻页按钮居中显示，提升界面美观度
- 优化：设置按钮移至底部栏右侧，布局更清晰
- 优化：底部导航栏改用 Grid 布局，进度条/翻页/设置三栏精确排列
- 优化：设置窗口改为模态窗口（grab_set + transient），防止误操作到主窗口
- 优化：文件选择对话框与模态窗口的 grab 交互，避免冲突
- 优化：设置窗口所有弹窗指定 parent，层级关系正确
- 优化：DownloadWindow 关闭时释放 grab
- 修复：切换保存路径后下载仍使用旧路径（改为模块对象动态读取配置）
- 修复：下载模块 config 导入方式从值拷贝改为引用，保存目录设置即时生效
- 修复：下载窗口关闭后主 UI 进度条不更新（轮询状态自动转移到主窗口）
- 修复：SettingsWindow 和 DownloadWindow 未保存引用导致 GC 回收窗口异常

## v1.10.0
- 新增：已下载作品分页浏览（每页 20 条，翻页/跳转/排序均支持）
- 新增：数据库缓存持久化声优（vas）、厂商（circle）、主封面（mainCoverUrl）
- 新增：works 表自动扩列（存量数据库无缝升级，首次启动自动添加新列）
- 优化：懒加载获取的声优/厂商信息自动写入 download_history 数据库
- 优化：已下载信息补全改为分批串行处理（每批 20 条 × 4 并发），避免 API 超时
- 优化：补全信息时实时显示进度（如"正在补全信息... (40/147)"）
- 优化：补全完成后不再强制跳转第 1 页，保持在当前页
- 优化：窗口启动时自动居中屏幕
- 修复：导航/刷新后 generation 过期导致刷新按钮永远卡住加载态
- 修复：补全队列超过 140 条时 Tkinter 控件过多崩溃（改为分页显示）
- 修复：标签数据含 None 值导致 join 崩溃
- 修复：hide_loading 误销毁控件池导致 TclError

## v1.9.0
- 新增：厂商搜索功能（点击详情页厂商名称即搜索该厂商全部作品）
- 新增：Canvas 彩色标签渲染（列表和详情标签使用 8 色循环彩色标签块）
- 优化：控件池复用机制（翻页不再销毁重建控件，只更新文本/图片）
- 优化：列表标签从多层嵌套 Frame+Label 改为单 Canvas 绘制，控件数减少 70%
- 优化：缩略图批量更新（所有图片一次回调主线程，不再逐个调度）
- 优化：翻页/搜索防抖机制（300ms 防抖 + 生成代数取消过期请求）
- 优化：Canvas 滚动区域重算合并（减少重复 bbox 调用）
- 优化：加载动画改用原生 Progressbar（不占用 Python 主线程）
- 优化：详情面板懒加载去抖（声优/厂商信息 300ms 延迟，快速切换自动取消）
- 优化：详情面板标签支持自动换行
- 修复：标签数据含 None 值导致 TypeError 崩溃
- 修复：hide_loading 误销毁控件池导致 TclError

## v1.8.0
- 新增：Aria2 真实下载进度显示（进度条、已下载/总大小、实时速度）
- 新增：API 请求自动重试（最多3次，指数退避 1s/2s/4s）
- 新增：配置文件防崩溃机制（损坏或缺失时自动使用默认值）
- 新增：详情面板显示声优（CV）和厂商（社团）信息
- 新增：缓存作品缺少声优/厂商时自动从 API 懒加载补充
- 优化：gui_app.py 拆分为 4 个 Mixin（1250行 → 469行，Mixin模式组合）
- 优化：标签搜索时搜索框内显示独立标签芯片，每个可单独关闭
- 优化：统一鼠标滚轮支持（左侧列表和右侧详情面板均支持滚动）
- 优化：go_to_page 页码跳转保留标签搜索/关键词搜索上下文
- 优化：关键词搜索时蓝色搜索芯片显示在搜索框内
- 修复：标签名含特殊字符（如 `/`）导致 404 错误
- 修复：详情面板鼠标滚轮在子控件上无响应的问题
- 修复：标签/关键词搜索后页码跳转丢失搜索状态

## v1.7.0
- 新增：关键词搜索（统一搜索框，自动识别 RJ ID 或关键词进行模糊搜索）
- 新增：详情面板显示声优（CV）和厂商（社团）信息
- 新增：详情面板可滚动（Canvas+Scrollbar），超出显示区域可滚动查看
- 新增：缓存作品缺少声优/厂商时自动从 API 懒加载补充
- 优化：统一鼠标滚轮支持（左侧列表和右侧详情面板均支持滚动）
- 修复：详情面板鼠标滚轮在子控件上无响应的问题

## v1.6.0
- 新增：已下载作品计数显示在顶部工具栏
- 优化：点击下载后立即标记已下载，无需等待任务提交完成
- 优化：下载任务提交流程，使用线程本地缓存 ServerProxy，复用 HTTP 连接
- 优化：16线程并发提交下载任务，速度翻倍
- 优化："只看已下载"首次加载先显示列表再异步获取封面，不再阻塞
- 优化：排序切换改为纯内存操作（sorted/reversed），毫秒级响应
- 优化：翻页按钮在"只看已下载"模式下自动禁用，防止误操作
- 修复：fetch_work_detail 未去掉ID前导零导致封面获取失败
- 修复：缺失封面补全时丢弃了标签、声优、社团等完整数据
- 修复：add_download 使用 INSERT OR REPLACE 重置下载时间戳
- 修复：更新作品信息使用新增的 update_work_detail，不破坏 created_at

## v1.5.0
- 新增：多标签组合搜索（点击多个标签同时筛选）
- 新增：其他语言版本ID可点击跳转
- 新增：标签搜索状态指示器
- 优化：长标题显示自动换行，修复显示不完整问题
- 优化：列表中显示其他语言版本ID

## v1.4.0
- 新增：标签搜索功能（点击任意标签快速筛选同类作品）
- 新增：搜索状态指示器和快速清除搜索按钮
- 优化：标签点击交互（鼠标变为手型指示）

## v1.3.0
- 新增：多线程下载（8线程并发添加任务）
- 新增：模态窗口（下载窗口只在主窗口上方，不遮挡其他程序）
- 优化：移除下载完成弹窗提示，改为状态栏显示
- 优化：下载后正确标记已下载状态
- 优化：使用 TreeSelector 类重构下载窗口选择逻辑

## v1.2.0
- 新增：双列表浏览（推荐/最新）
- 新增：ID搜索功能
- 新增：图片缓存机制
- 新增：下载历史管理
- 新增：筛选排序功能
